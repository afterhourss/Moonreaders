

function Button({text}) {
  return (
    <button className="px-7 py-2 rounded-lg bg-black text-white hover:bg-gray-900 hover:transition-all font-semibold">{text}</button>
  )
}

export default Button